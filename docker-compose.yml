services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: mementorize
      POSTGRES_PASSWORD: mementorize
      POSTGRES_DB: mementorize
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mementorize"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
