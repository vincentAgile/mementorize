# packages

Réservé au code partagé entre `apps/api` et `apps/web` (configuration ESLint/TypeScript commune, types partagés) — introduit à partir de la **phase 5** lorsque le modèle de contenu se généralise.
